// This component is no longer used and can be safely deleted.
// The theme is hardcoded to dark mode in index.html.
import React from 'react';

const ThemeToggle: React.FC = () => {
  return null;
};

export default ThemeToggle;
